import React from 'react';
import RootNav from '../index';
import { shallow, render } from 'enzyme';

describe('components/RootNav', () => {
  it('should render a list of nav elements', () => {
  });
});
